# Claim 2 evaluation

Verdict: **VERIFIED**.

The exact conditional-mean misspecification is linear in `alpha` for CTL and
in `c alpha` for LTC. The reconstructed square-loss theorem squares this
misspecification, yielding `alpha^2` and `c^2 alpha^2`. The old primary source
contains `alpha` and `c alpha` on the same squared-error scale. The independent
checker exhaustively enumerates 1,600 exact rational configurations.
