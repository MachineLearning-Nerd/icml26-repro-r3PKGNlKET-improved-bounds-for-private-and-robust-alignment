# Claim 3 evaluation

Verdict: **VERIFIED**

The exact identity, attainable upper bound, and estimator-independent lower
bound agree on the `c(epsilon)^2/n` estimation scale. The independent checker
must pass 68 cases, and the wrong-scaling control must exit 1. The fixed
runner fails if either condition is violated.
