# Claim 4 method

The primary verifier derives both conditional means symbolically and supplies
endpoint witnesses. The independent checker enumerates the full generative
process—clean Bernoulli label, Huber replacement, randomized response—in both
orders over 1,600 rational configurations. It does not import primary code.

The negative control incorrectly uses CTL's scaled replacement mean for LTC;
the LTC formula and tight witness checks fail and the process exits nonzero.
